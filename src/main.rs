// Most code here is supposed to be run as a test case with `cargo test` so avoid dead code warnings.
#![allow(dead_code)]

// All solutions go here.
mod solutions;

// Dummy main
fn main() {
    println!("Hello, world!");
}
